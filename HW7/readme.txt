	
	這次的程式，我是利用matrix寫出。

	首先第一部分是讀入資料，讀入並存近二維陣列中，為了方便把二維陣列傳進我宣告的函式中
	(因為total event是變動的)所以我用了指標宣告動態二維陣列。
	在這部分我也我宣告一個變數count，算出總共有幾個activity。

	第二部分我呼叫宣告的ee le函式找到 ee le值，先從起點找ee值而後從終點導回le值

	第三部份我根據地第部分的count，把編號、e()值、l(值)、及slack值找到。
	另外因為助教有註解ai可以印成(x,y)格式，所以為了檢視方面我也一起把x y印出來。