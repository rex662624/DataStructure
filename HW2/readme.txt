作業一:

在這個程式中，我的程式架構如下:

1.首先讀入檔案，以及想要的a b數量(我先用一次fscanf，讀入n*n矩陣的n值)

2.再來用for 迴圈，從00 01 02 03 10 11 ......依序讀入每個元素，並傳入value函式。

3.依次印出e矩陣，及行數列數即為結果，最後關閉開啟的檔案。

在value函式中，我用index表示現在讀入的元素應該擺入e矩陣的第幾個位置。

第一個if為若讀入在a b band外的範圍的數字，每次都覆蓋存在garbage，不會用到。

第二個if為若在主對角線及上方b-1內的數，該如何處置。

第三個if為若在主對角線下方a-1內的數，該如何處置。




作業二:

在這個程式中，我的程式架構如下:

1.首先讀入m n x y 其中m  要介於2和40之間，n 要介於2到20之間。x y不能超過讀入的m n 範圍。並宣告imove[]、jmove[]表示走動的7個情況。

2.我製作了2個函數，一個為judge，用來判斷全部是否已經被走過一次，另一個為randomwalk，用來作隨機走動用。

3.因為我要把矩陣傳進matrix函式中，用來計算跑過每格的次數，而又由於m n是變動數值，不能直接宣告二維矩陣傳入，故改用指標方式宣告。

4.judge矩陣中，用for迴圈跑過所有的matrix陣列，若有一次讀到0，就回傳1繼續randomwal。k

5.randomwalk中，我用虛擬亂數，跟8取餘數得到0~7的數字。以實現隨機走動。若走動後的結果發現超過範圍，就不做任何事等待下次進入函式
，若走動結果在範圍內則步數+1，格子的次數也+1。


